# Datasource is configured in application.yml via DATABASE_URL / DATABASE_USERNAME / DATABASE_PASSWORD.
spring:
  jpa:
    show-sql: true

logging:
  level:
    root: INFO
    com.protrack: DEBUG
    org.flywaydb: INFO
