package com.protrack.project.domain;

/** Kind of publication (stored as the enum name in projects.publication_type). */
public enum PublicationType {
	STEM_TEXTBOOK,
	MONOGRAPH,
	JOURNAL,
	REFERENCE
}
